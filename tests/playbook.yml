---
- hosts: localhost
  sudo: True
  roles:
    - java
    - sbt
    - scala
    - ./
  vars:
    mesos_version: "0.18.0"
    mesos_cluster_name: testing
    mesos_install_mode: "master-slave"
- include: tests/integration-tests.yml
