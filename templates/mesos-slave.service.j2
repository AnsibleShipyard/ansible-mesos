# {{ansible_managed}}

[Unit]
Description=mesos slave

[Service]
Type=simple
User={{ mesos_owner }}
Group={{ mesos_group }}
ExecStart=/usr/bin/mesos-init-wrapper slave
TimeoutSec=300

[Install]
WantedBy=multi-user.target
