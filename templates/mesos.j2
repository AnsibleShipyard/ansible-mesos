# mesos defaults file
# Generated by Ansible for {{ansible_fqdn}}

CLUSTER={{mesos_cluster_name}}
IP={{ansible_default_ipv4.address}}
LOGS={{mesos_log_location}}
ULIMIT="{{mesos_ulimit}}"
ZK="zk://{{groups.mesos_masters | join(':' + mesos_local_port + ',')}}:{{ mesos_local_port }}/mesos"
MASTER="zk://{{groups.mesos_masters | join(':' + mesos_local_port + ',')}}:{{ mesos_local_port }}/mesos"
OPTS="{{mesos_options}}"
WORK_DIR="{{ work_dir }}"
QUORUM="{{ quorum }}"
