---
  - name: download mesos v{{version}}
    get_url: url={{download_url}} dest={{dest_file}}

  - name: Install Mesos Debian Package
    shell: dpkg -i {{dest_file}}

  - name: Mesos default config file
    template: src=templates/mesos.j2 dest=/etc/default/mesos
